    // Verificar si hay un mensaje de error y mostrar una alerta
    var errorMessage = document.querySelector('.error');
    if (errorMessage) {
        alert(errorMessage.textContent.trim());
    }
